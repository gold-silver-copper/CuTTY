image: archlinux

packages:
  - pkg-config
  - cmake
  - make
  - freetype2
  - fontconfig
  - libxcb
  - libxkbcommon
  - scdoc

environment:
  PATH: /home/build/.cargo/bin:/usr/bin/

tasks:
  - rustup: |
      curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain stable --profile minimal
  - rustfmt: |
      rustup toolchain install nightly -c rustfmt
      cargo +nightly fmt -- --check
  - man-pages: |
      cat extra/man/cutty.1.scd | scdoc > /dev/null
      cat extra/man/cutty-msg.1.scd | scdoc > /dev/null
      cat extra/man/cutty.5.scd | scdoc > /dev/null
      cat extra/man/cutty-bindings.5.scd | scdoc > /dev/null
  - test: |
      cargo test
  - oldstable: |
      oldstable=$(cat Cargo.toml | grep "rust-version" | sed 's/.*"\(.*\)".*/\1/')
      rustup toolchain install --profile minimal $oldstable
      rustup default $oldstable
      cargo test
  - clippy: |
      rustup component add clippy
      cargo clippy --all-targets
  - feature-wayland: |
      cd cutty
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=wayland
  - feature-x11: |
      cd cutty
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=x11
